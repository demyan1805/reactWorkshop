import React from 'react';
import gif from '../Spinner.gif';

const Spinner = () => {
    return (
        <>
            <img src={gif} alt="spinner"></img>
        </>
    )
}

export default Spinner;